# Full URL of the Next.js dashboard (Login, Buy now, webhooks)
PUBLIC_DASHBOARD_URL=http://localhost:3000

# Supabase (required for live CMS — without these, the site uses fallback data in src/data/templates.ts)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
