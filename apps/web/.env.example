# DEV.ECOM — Astro storefront

# Optional for now — site uses fallback template data without these:
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Optional — defaults to /dashboard; set to your live Next.js URL when deployed:
PUBLIC_DASHBOARD_URL=http://localhost:3000
